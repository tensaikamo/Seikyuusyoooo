name: Final legacy snapshot settings gate

on:
  pull_request:
    branches:
      - main
    types:
      - synchronize

permissions:
  contents: write

jobs:
  patch:
    if: github.head_ref == 'agent/invoice-hardening-final-audit'
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout isolated audit branch
        uses: actions/checkout@v4
        with:
          ref: agent/invoice-hardening-final-audit
          fetch-depth: 0
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Apply guarded final patch
        run: python3 tools/final_legacy_settings_patch.py
      - name: Check syntax
        run: node --check app.js
      - name: Run all tests
        run: node --test tests/*.test.js
      - name: Remove one-shot patch machinery
        run: rm -f tools/final_legacy_settings_patch.py .github/workflows/final-legacy-settings-gate.yml
      - name: Commit verified result
        run: |
          git config user.name "invoice-hardening-bot"
          git config user.email "invoice-hardening-bot@users.noreply.github.com"
          git add app.js tests/core-invariants.test.js tools/final_legacy_settings_patch.py .github/workflows/final-legacy-settings-gate.yml
          git commit -m "fix: preserve legacy snapshot settings evidence"
          git push origin HEAD:agent/invoice-hardening-final-audit
